const config = {
  spreadsheetId: 'gibberish_from_sheet_url',
  range: 'hello!A:F',
  tokenPath: 'sheets/token.json',
  credentialsPath: 'sheets/credentials.json',
  outputPath: 'sheets/quotes.json'
};

module.exports = config;